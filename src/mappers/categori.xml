<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="categori">
	<insert id="addcate" parameterType="java.util.HashMap">
		insert into issue values (#{it},#{game},#{politics},#{sports},#{food},#{movie},#{life},#{etc})
	</insert>	
	</mapper>